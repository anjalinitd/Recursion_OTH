<!DOCTYPE html>
<html lang="en">
<head>
  <style>
    /* Set black background color, white text and some padding */

    body{
       height: 100%;
   width: 100%;

      background-image: url("http://www.guoguiyan.com/data/out/144/69896840-papyrus-wallpapers.jpeg");
      overflow-x:hidden;
    overflow-y:hidden;
      background-size:100% 100%;
        background-repeat: no-repeat;/*we want to have one single image not a repeated one*/
   background-size: cover;
      padding-left: 50px;
      margin-top: 130px;
    }

    #answerForm{
          width: 20em;  height: 2em;
    }

  </style>
</head>
<body>

<!-- navbar -->
<div class="navbar-wrapper">

 <nav class="navbar navbar-inverse navbar-fixed-top">
 <div class="navbar-inner">
  <div class="container-fluid">
    <div class="navbar-header">
      <h1 class="brand"> <a  href="#">Quaerere</a> </h1>
    </div>
    <ul id="menu-main" class="nav navbar-nav navbar-right">
    <li><a title="team" href="home">Home</a></li>
        <% if current_user!=nil %>
         <li><a title="team" href="leaderboard">Leaderboard</a></li>
         <% end %>
          <li class="dropdown">
        <a class="dropdown-toggle" data-toggle="dropdown" href="#">Rules
        <span class="caret"></span></a>
        <ul class="dropdown-menu">
          <li><a>1. Write all answers in lowercase.</a></li>
          <li><a>2. Don't include space between two words.</a></li>
          <li><a>3. Try to link the question with the image and audio.</a></li>

        </ul>
      </li>

      <% if current_user!=nil %>
      <li class="dropdown">

         <a  class="dropdown-toggle" data-toggle="dropdown" href="#"><%= current_user.name %> <span class="caret"></span></a>
       <ul class="dropdown-menu">

          <li><%= link_to "Sign out", 'sign_out', method: :delete , class: "nav-link"%></li>
        </ul>
      </li>
      <% else %>
      <li><a>Login ::</a></li>
      <li ><a href="auth/facebook"> <img border="0" alt="facebook" src="fb.jpg" class="img-circle"width="30" height="30"></a></li>
      <li ><a href="/auth/google_oauth2"> <img border="0" alt="Gmail" src="gmail.jpg" class="img-circle" width="30" height="30"></a></li>
      <% end %>
    </ul>
  </div>
  </div>
</nav>
</div>







<% if current_user %>
<div class="container" id="userPro">
<%= image_tag(current_user.image_url, size: '50x50') %>&nbsp;&nbsp;
<%= current_user.name %><%= ", "%><%= current_user.college %>

<p style="font-size:20px; float:right;"><%= "Score : "%><span id="myScore"><%= current_score %></span>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;<%= "Level : "%><span id="myLevel"><%= (current_score==0)?1:current_score/10+1 %></span></p>


<div id="hunt">
<% if @question!=nil %>
	<div id="myQuestion">
	<h2><%= @question.statement %></h2>
	<span id="myImage"><%= image_tag(@question.imageurl)%></span>
	</div>
	<br /><br />


	<form id="answerForm">
	<input type="text" name="answer" id="myAnswerText" placeholder="Type your answer here">
	<input type="submit" name="submit" id="myAnswer">
	</form>
<% else %>
<br /><br />
<h2>Wait for upcoming questions</h2>
<% end %>
</div>


<div id="myResult">
</div>

<% end %>
